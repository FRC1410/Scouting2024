<%= form_with(model: match) do |form| %>
  <% if match.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(match.errors.count, "error") %> prohibited this match from being saved:</h2>

      <ul>
        <% match.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :red_alliance_id, style: "display: block" %>
    <%= form.text_field :red_alliance_id %>
  </div>

  <div>
    <%= form.label :blue_alliance_id, style: "display: block" %>
    <%= form.text_field :blue_alliance_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
